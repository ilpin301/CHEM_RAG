# Normalize line endings in history; check out native on each platform.
* text=auto

# PowerShell is edited on Windows and run by Windows PowerShell - keep CRLF.
*.ps1 text eol=crlf

# Binary corpus - never diffed, never normalized.
*.pdf binary
*.rar binary
